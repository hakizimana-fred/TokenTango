function processToken(tokenAddress: string, name: string, symbol: string) {}

function curry(func: Function) {
  return function curried(...args: any) {
    if (args.length >= func.length) {
    } else {
    }
  };
}
